name: Poll SR Leaderboards

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: poll
  cancel-in-progress: false

jobs:
  poll:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
      - name: Poll Fruma
        env:
          CONFIG_FILE: config.json
          HISTORY_FILE: snapshots.json
        run: node poll.js
      - name: Poll NoL
        env:
          CONFIG_FILE: config-nol.json
          HISTORY_FILE: snapshots-nol.json
        run: node poll.js
      - name: Commit and push if changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add snapshots.json snapshots-nol.json
          if git diff --staged --quiet; then
            echo "No changes"
          else
            git commit -m "chore: append snapshots"
            for i in 1 2 3; do
              git pull --rebase origin main && git push && break
              echo "Retry $i"
              sleep 2
            done
          fi
